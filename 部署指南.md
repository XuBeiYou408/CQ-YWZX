# CQ-YWZX 本地工具集 · 新电脑部署指南

本目录含两个**各自独立**、可接入 WorkBuddy 的本地 MCP 工具——两个项目互不依赖，
可只部署其中一个（每个项目自带的安装脚本独立完成自己的全部安装与体检）：

| 项目 | MCP 服务名 | 功能 | 模型依赖 |
|---|---|---|---|
| `kb-tool` | enterprise-knowledge-base | 企业知识库检索（年假/考勤/报销等制度问答） | **无**（纯本地 BGE+BM25 检索） |
| `合同审查` | contract-reviewer | 合同审查 / Word 红线批注版 / 法条判例 / 起草 | 轻量轨无；**完整轨需 LM Studio** |

> 两个项目之间没有统一的部署脚本：哪个项目要用，就进哪个项目文件夹运行它自己的
> 「一键安装到WorkBuddy.bat」（或 `python install_to_workbuddy.py`）。

> 📁 本目录共含 5 个项目：上表两个为 **WorkBuddy MCP 工具**（本指南适用）；
> 另有 `企业本地知识库`（Vue 3 前端完整版 RAG 问答系统）、`简历筛查`、`销售周报`
> 三个**独立运行的 Web 项目**——它们不接入 WorkBuddy，各自双击项目内的
> `一键启动.bat` 即可运行，详见各自项目内的 README.md。

---

## 一、新电脑前置条件

1. **Windows + Python 3.10+**（`py -3` 或 `python` 可用；安装时勾选 "Add to PATH"）
2. **WorkBuddy** 已安装并登录
3. **网络**（首次安装依赖需联网；kb-tool 依赖较大，约 10~30 分钟）
4. **LM Studio + 至少一个 instruct 模型**（**仅合同审查完整轨需要**；建议上下文设 64k、开启 GPU 卸载）
   - kb-tool 完全不需要 LM Studio

## 二、拷贝什么

| 内容 | 是否拷贝 | 说明 |
|---|---|---|
| 两个项目文件夹（源码、`data/`、`deploy/`） | ✅ 必须整体拷贝 | **kb-tool 的 `data/` 含 BGE 模型和知识库索引，漏拷则检索不可用** |
| `.venv` 文件夹 | ❌ 不用拷 | venv 不可跨机使用（内置绝对路径）；即使拷了，安装脚本检测到损坏会自动删除重建 |
| `*.bak` / `*.bak-*` 备份文件 | ❌ 不用拷 | 历史修改备份，仅留档 |

## 三、安装步骤（每个项目独立，双击即用）

进入要使用的项目文件夹，**双击「一键安装到WorkBuddy.bat」**（等效命令：`python install_to_workbuddy.py`）。
两个项目操作完全相同、互不影响，只需装你要用的那个。脚本自动完成（幂等，可重复运行）：

1. 创建 venv 并安装依赖（从 requirements.txt；venv 损坏/缺失时自动重建）
2. 注册 MCP 服务到 WorkBuddy 的 mcp.json（合并写入，保留其他已有服务）
3. 部署路由 Skill（让对话模型"看得见"该走哪个工具）
4. 写入用户记忆路由规则（禁用 WebSearch 回答公司制度/合同问题）
5. **端到端体检**：真实启动本项目的 MCP 服务 → 协议握手计时 → 列出工具 →
   实际调用一次（kb-tool 检索"年假制度"；合同审查调 `ping` 环境检测），
   不经过 WorkBuddy 即可确认服务本身完全可用；同时检查信任状态，已信任则免提示
6. 合同审查版额外探测 LM Studio 并报告可用模型（不在线只警告，不阻断）

可选参数：`--mcp-only` 只更新 mcp.json；`--no-verify` 跳过端到端体检。

> 两个项目的 bat 行为完全一致：**无论 `.venv` 是否已存在，都执行上面的完整安装脚本**
> （venv 有则复用、无则创建），避免出现"venv 已存在的机器上 Skill 漏装"。
> kb-tool 另配 `图形化配置助手.bat`：打开 GUI 做卸载 / 修改路径 / 状态自检 / 端到端体检
> （它会自动挑选带 tkinter 的解释器——`.venv` 的基础 Python 可能不含 tkinter，此时 GUI 用系统 Python 显示，MCP 服务仍由 `.venv` 承载）。

## 四、启用与验证

1. **信任服务**：WorkBuddy 连接器管理页 → 自定义连接器 → 对 `enterprise-knowledge-base` 和 `contract-reviewer` 各点一次「信任」（或重启 WorkBuddy）。
2. **验证 kb-tool**：新开会话问"公司年假制度"——应自动检索知识库并回答（来源为内部制度文档，而非网上的法定天数）。
3. **验证合同审查**：
   - 秒级：发送一条合同条款让"扫描风险"；
   - 完整轨：拖入合同 .docx 说"帮我审查"——约 1~2 分钟出审查报告 + 原文件同目录的批注版 Word。
4. **合同审查环境报错时**：让模型调用 `ping` 工具体检（LM Studio 在线状态 / 可用模型 / 处理指引）。

## 五、项目更新后如何同步（Skill / 记忆规则 / 代码）

项目里的 Skill、记忆规则或代码有更新后，**直接重跑对应项目的「一键安装到WorkBuddy.bat」**即可，
脚本幂等，重复运行安全：

| 更新内容 | 重跑 bat 后的效果 |
|---|---|
| `deploy/skills/<skill>/SKILL.md`（路由路标） | 用项目内最新副本**覆盖** `~/.workbuddy/skills/<skill>/SKILL.md` |
| `install_to_workbuddy.py` 里的 `MEMORY_RULES*` | 用最新内容**替换** `~/.workbuddy/MEMORY.md` 对应托管块（不会重复堆叠） |
| `mcp_server.py` 等代码 | 无需额外操作，`mcp.json` 始终指向当前项目的 `.venv` 与 `mcp_server.py` |
| — | 收尾自动跑一次端到端体检，确认改动没把服务弄坏 |

> 注意：**只改 Skill 文件、不重跑 bat 的话，`~/.workbuddy/skills` 下仍是旧版本**——
> 用户级 Skill 是安装时拷贝过去的副本，不是软链接。

## 六、常见问题

| 现象 | 处理 |
|---|---|
| 新会话首问没走工具 | 追问一句"用企业知识库/合同审查工具再查"（第二轮必中）；根治靠路标已部署，偶发属小模型随机性，可换更大的对话模型 |
| 完整审查报"LM Studio 不可达" | 启动 LM Studio 并加载模型；端口非 1234 时设环境变量 `LM_STUDIO_BASE_URL`（如 `http://127.0.0.1:8080/v1`） |
| 想固定审查用某个模型 | 设环境变量 `AGENT_MODEL`（如 `qwen3.6-35b-a3b`）；不设则引擎自动优选已加载模型 |
| 检索报模型/索引错误 | 检查 kb-tool 的 `data/models` 与索引目录是否完整拷贝 |
| 依赖安装失败 | 检查网络后重跑安装脚本，或手动：`"项目\.venv\Scripts\python.exe" -m pip install -r requirements.txt` |
| 收尾回答被截断（审查场景） | LM Studio 把加载上下文调大到 65536 |

## 七、模型建议（对话模型，WorkBuddy 左下角选择）

- **本地**：qwen3.6-35b-a3b（工具调用最可靠，需 32GB 内存）或 gemma-4-e4b（最快最省内存，偶发路由犹豫）
- **云端**：任意支持 function calling 的模型（可靠性最好）
- 建议在 LM Studio 开启"保持模型在内存中"，避免每次冷加载
