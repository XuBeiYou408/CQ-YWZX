# ============================================
# 敏感文件 — 绝不能提交到 Git
# ============================================
.env
.env.*
!.env.example

# ============================================
# Python
# ============================================
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
!**/frontend/dist/
!**/frontend/dist/**
build/
.eggs/
*.egg
.venv/
venv/
env/

# node_modules/
node_modules/
!.env.example

# ============================================
# IDE / 编辑器
# ============================================
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ============================================
# AI 工具 & 方案/报告导出文件 (绝不上传)
# ============================================
.claude/
.workbuddy/
*优化方案*.md
*升级方案*.md
*计划书*.md
*改造计划书*/
*report*.md
*plan*.md
*walkthrough*.md
*.log

# ============================================
# 运行时数据与大模型权重文件 (超大体积禁止推送 GitHub)
# ============================================
*.db
*.sqlite3
*.pkl
*.index
*.faiss
*.sig
faiss_index/
faiss-db/
data/faiss_db/
data/models/
*.bin
*.safetensors
*.pt
*.pth
*.onnx
*.gguf

# 保留企业知识库制度与资料文档
!data/documents/
!data/documents/**

# ============================================
# 各项目「运行时数据」— 一律不入库（含用户隐私）
# 这些内容由应用启动时自动创建/以代码内预置数据重建，或由使用者在界面中填写：
#   · 简历筛查 data/store.json  → 候选人库、上传的简历原文、微聊记录
#   · 简历筛查 config.json      → 界面里填的云端 api_key 会写入此文件
#   · 销售周报 data/*.db        → 销售台账（真实客户/金额）
#   · 销售周报 uploads/exports  → 上传明细与 Word/Markdown 导出产物
#   · 销售周报 config.json      → 同上，含 api_key
# ============================================

# 简历筛查（该项目无独立 .gitignore，由本文件兜底）
# data/ 下只保留「开发自测基线快照」（纯预置演示数据，供 一键恢复数据.bat 使用）；
# store.json 含上传简历原文与候选人数据，一律不入库。
简历筛查/data/*
!简历筛查/data/store.baseline.json
简历筛查/uploads/*
!简历筛查/uploads/.gitkeep
简历筛查/config.json

# 销售周报（项目内 .gitignore 已有同名规则，此处作跨目录兜底）
销售周报/data/
销售周报/uploads/
销售周报/exports/
销售周报/config.json

# 企业本地知识库 / kb-tool：真实 .env（含云端密钥），范本 .env.example 保留
企业本地知识库/.env
kb-tool/.env

# 通用临时文件
*.tmp

