# CQ-YWZX 项目合集仓库

本项目仓库用于集中归档与管理相关企业级业务智能体系统及核心交付模块。

---

## ⚙️ 全局运行环境与自举式架构规范

- **Python 运行环境要求**：**建议 Python 3.11 – 3.13**（推荐从 [python.org](https://www.python.org/downloads/) 下载官方安装包，安装时务必勾选 **`Add Python to PATH`** 与 **`py launcher`**）。
- **自举式一键启动 (Self-Bootstrapping)**：
  - 各项目**完全独立、物理隔离、互不依赖**，各自在项目目录下维护专属的 `.venv` 虚拟环境与 `requirements.txt`。
  - 在全新克隆的代码库中，直接双击各项目目录下的 **`一键启动.bat`**，脚本将优先调用 `py -3`（防御微软商店假占位符），自动创建当前项目的 `.venv`、自动安装完整依赖并拉起服务。
  - 脚本具备**严格幂等性**，后续日常使用双击可秒级启动，无需重复安装依赖。

---

## 📂 项目模块结构与分类

| 项目目录 | 系统名称 | 技术栈 / 架构 | 默认端口 | 交付状态 | 核心亮点说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **[企业本地知识库/](./企业本地知识库)** | 企业本地知识库问答与智能 Agent 系统 | FastAPI + LangChain + FAISS + BM25 + Vue 3 + LM Studio | `8010` | ✅ 已交付 | 支持企业内部制度文档检索、双路混合检索、本地离线与云端大模型动态切换、流式问答与会话管理。 |
| **[合同审查/](./合同审查)** | 合同审查 AGENT · 企业级合同合规审查系统 | FastAPI + Vue 3 + Tailwind CSS + OOXML Lite + LM Studio | `8020` | ✅ 已交付 | 支持100%离线端侧高隐私审查、16类门禁与20项最高院判例检索、现代SaaS风控工作台及Word原生修订批注导出。 |
| **[简历筛查/](./简历筛查)** | RecruitAI · 企业智能招聘与简历初筛智能体工作台 | FastAPI + Vue 3 + Tailwind CSS (Stitch 1:1) + LM Studio / Cloud LLM | `8030` | ✅ 已交付 | 1:1复刻Google Stitch设计体系，支持8维招聘硬性门槛过滤、A4规范原版履历主视图、双栏即时沟通中心、委婉回绝全闭环与多模型端云热切换。 |
| **[销售周报/](./销售周报)** | SalesAgent · 企业销售周报自动汇总智能体驾驶舱 | FastAPI + Vue 3 + Tailwind CSS + python-docx + openpyxl + LM Studio | `8040` | ✅ 已交付 | 采用确定性算术核算与大模型战略洞察双轨架构，支持主管全屏查看销售100%完整原件周报文档、四维经营KPI看板、核心突破与风险雷达提炼、就地润色及标准Word(.docx)/Markdown一键导出。 |

---

## 📋 各项目前置条件与运行指引

每个子项目均为独立自治工程，请查阅对应项目目录下的 `README.md` 与前置配置：

### 1. [企业本地知识库](./企业本地知识库) (端口: `8010`)
- **前置条件与系统要求**：
  - **Python**：建议 3.11 – 3.13。
  - **Node.js 环境**：前端基于 Vue 3 + Vite 构建。若本地无静态构建产物，需要 Node.js (v18+) 执行 `cd frontend && npm install && npm run build`（仓库已内置构建产物时可直接开箱运行）。
  - **嵌入与重排模型**：系统依赖 BGE 向量模型（`BAAI/bge-large-zh-v1.5` 与 `bge-reranker-large`），启动时默认通过镜像源自动加载。
  - **大模型支持**：后台需启动 LM Studio（默认端口 `1234`）或在 `.env` 中填入云端 API Key（如 DeepSeek）。
- **启动方式**：进入 `企业本地知识库/` 目录，双击运行 **`一键启动.bat`**。

### 2. [合同审查 AGENT](./合同审查) (端口: `8020`)
- **前置条件与系统要求**：
  - **Python**：建议 3.11 – 3.13。
  - **依赖管理**：双击 `一键启动.bat` 自动自举创建 `.venv` 并补全安装所有依赖（含 PyMuPDF、python-multipart、python-docx、FastAPI 等）。
  - **大模型支持**：后台启动 LM Studio 并开启 Local Server（默认地址 `http://127.0.0.1:1234/v1`），或在工作台右上角切换并配置云端 API Key。
- **启动方式**：进入 `合同审查/` 目录，双击运行 **`一键启动.bat`**。

### 3. [RecruitAI 简历初筛智能体](./简历筛查) (端口: `8030`)
- **前置条件与系统要求**：
  - **Python**：建议 3.11 – 3.13。
  - **依赖管理**：双击 `一键启动.bat` 自动自举创建 `.venv` 并补齐依赖（含 python-multipart、PyMuPDF、FastAPI 等）。
  - **大模型支持**：本地 LM Studio (`qwen3.8-27b`) 或在工作台模型设置弹窗中填入云端 API Key (DeepSeek / 通义千问等)。
- **启动方式**：进入 `简历筛查/` 目录，双击运行 **`一键启动.bat`**。

### 4. [SalesAgent 销售周报智能体](./销售周报) (端口: `8040`)
- **前置条件与系统要求**：
  - **Python**：建议 3.11 – 3.13。
  - **依赖管理**：双击 `一键启动.bat` 自动自举创建 `.venv` 并补全安装所有依赖（含 openpyxl、python-docx、python-multipart 等）。
  - **大模型支持**：本地 LM Studio (`qwen3.8-27b`) 或云端大模型热切换。
- **启动方式**：进入 `销售周报/` 目录，双击运行 **`一键启动.bat`**。
