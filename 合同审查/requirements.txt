fastapi>=0.100.0
uvicorn[standard]>=0.23.0
openai>=1.0.0
pydantic>=2.0.0
python-docx>=1.0.0
PyMuPDF>=1.23.0
python-multipart>=0.0.9
# 必须锁 <2：MCP Python SDK 2.0（2026-07-27）把 FastMCP 改名为 MCPServer、
# 导入路径迁到 mcp.server.mcpserver 且无兼容别名，本项目的
# `from mcp.server.fastmcp import FastMCP` 在 2.x 下会 ModuleNotFoundError，
# 服务在 import 阶段即崩溃（客户端只表现为「连接失败」）。
mcp[cli]>=1.0.0,<2
httpx>=0.27.0
